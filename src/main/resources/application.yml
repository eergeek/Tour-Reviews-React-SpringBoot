# Hibernate Settings
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        use_sql_comments: true
        format_sql: true

# Enable H2 console, so we can view the data in web browser
  h2:
    console:
      enabled: true
      path: /h2-console

# BELOW database, we dont need to set them cuz spring boot
# see H2 dependency in out POM
#spring:
#  datasource:
#    url: jdbc:h2:mem:example
#    username: sa
#    password:
#  jpa:
#    database-platform: org.hibernate.dialect.H2Dialect